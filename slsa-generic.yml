name: SLSA generic generator
on:
  workflow_dispatch:
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      digests: ${{ steps.hash.outputs.digests }}

    steps:
      - uses: actions/checkout@v4

      # ========================================================
      # Шаг 1: Build артефактов (твоих файлов)
      # ========================================================
      - name: Prepare artifacts
        run: |
          # Копируем файлы для подписи
          cp artel_mini.py artifact_artel_mini.py
          cp slsa-generic.yml artifact_slsa-generic.yml

      # ========================================================
      # Шаг 2: Генерация subject для SLSA
      # ========================================================
      - name: Generate subject for provenance
        id: hash
        run: |
          set -euo pipefail
          files="artifact_artel_mini.py artifact_slsa-generic.yml"
          # Создаем sha256 и кодируем base64
          digests=$(sha256sum $files | awk '{print $1}' | base64)
          echo "digests=$digests" >> $GITHUB_OUTPUT

  provenance:
    needs: build
    permissions:
      actions: read
      id-token: write
      contents: write
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v1.4.0
    with:
      base64-subjects: "${{ needs.build.outputs.digests }}"
      upload-assets: true